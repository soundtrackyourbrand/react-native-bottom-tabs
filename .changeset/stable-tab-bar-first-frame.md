---
'react-native-bottom-tabs': patch
---

Keep Android tab labels in place while the tab bar loads: text appearance and tint colors are applied before the first frame, and items whose icon is still loading reserve the icon's space
