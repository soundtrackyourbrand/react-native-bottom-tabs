---
'@soundtrackio/react-native-bottom-tabs': patch
---

Share one Coil image loader across Android tab bars, built off the main thread on first use, instead of creating one per tab bar while it is being created
